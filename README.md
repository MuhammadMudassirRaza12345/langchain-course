python -m uv init
python -m uv --version
python -m uv venv
python -m uv sync

python -m uv pip install -r requirements.txt

pip install langchain langchain-openai langchain-tavily 

LangChain- Agentic AI Engineering with LangChain & LangGraph

git push -u origin project/hello-world --force